{
  "name": "whatsapp-clone-monorepo",
  "version": "1.0.0",
  "description": "Root package for deploying the WhatsApp clone to Render.",
  "scripts": {
    "build": "cd frontend && npm install --production=false && npm run build",
    "start": "cd backend && npm install && node src/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
