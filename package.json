{
  "name": "whatsapp-clone-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for WhatsApp Clone",
  "scripts": {
    "start": "node backend/src/server.js",
    "postinstall": "npm install --prefix frontend --production=false && npm run build --prefix frontend && npm install --prefix backend"
  },
  "engines": {
    "node": "22.x"
  }
}
